pages:
    stage: deploy
    image: node:latest
    script:
      - npm install
      - npm run build-storybook
      - mkdir .public
      - cp -r * .public
      - mv .public public
    artifacts:
      paths:
        - public
    only:
      - develop