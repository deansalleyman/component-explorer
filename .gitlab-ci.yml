pages:
    stage: deploy
    before_script:
      - npm install
      - npm run build-storybook
    script:
      - mkdir .public
      - cp -r * .public
      - mv .public public
    artifacts:
      paths:
        - public
    only:
      - develop